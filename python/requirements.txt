# Gesture Exhibit - Python detector dependencies
# Python 3.10 - 3.13 (mediapipe 0.10.35 ships a py3 wheel that installs on 3.13;
# if import fails at runtime on your machine, use a 3.12 venv - see README).

mediapipe==0.10.35        # Tasks Vision HandLandmarker (21-point landmarks; open/fist from skeleton geometry)
opencv-python>=4.9        # webcam capture, homography, preview window
Pillow>=10                # camera_settings.py canlı önizleme (PIL.Image / ImageTk)
numpy>=1.26               # array math (also a mediapipe dependency)
python-osc>=1.8           # OSC 1.0 encoding that Unity/OscJack expects
websockets>=12            # tarayıcı WebSocket köprüsü (net/ws_sender.py); SADECE config.json net.transport = ws|both iken gerekir
# One Euro Filter is implemented in-repo (smoothing/one_euro.py) - no extra dependency.
